class: OtsoPushSubscription
table: otso_push_subscriptions
fields:
  pubkey: String
  relay: String
  token: String
indexes:
  otso_push_subscription_unique_idx:
    fields: pubkey, relay, token
    unique: true
