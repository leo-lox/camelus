class: BloomFilterEvent
table: bloom_filter_events
fields:
  size: int
  numHashFunctions: int
  bitArray: String
  createdAt: DateTime
  name: String?
  description: String?

indexes:
  bloom_filter_events_crated_at_idx:
    fields: createdAt
    unique: false