class: PushSubscription
table: push_subscriptions
fields:
  pubKey: String
  relay: String
  token: String
  kinds: List<int>?
indexes:
  subscription_unique_idx:
    fields: pubKey, relay, token
    unique: true
